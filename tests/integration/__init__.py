"""
Integration tests for Proxy Agent Platform.

This package contains integration tests that verify the interaction between
different components of the system, including API endpoints, database operations,
and service layer integrations.

Integration tests require:
- Backend server running (for HTTP integration tests)
- Database access
- External service mocks or test credentials (as needed)

Run with:
    uv run pytest tests/integration/ -v
"""
