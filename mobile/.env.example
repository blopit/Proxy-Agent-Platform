# ============================================================================
# Mobile App Environment Configuration - EXAMPLE
# ============================================================================
# Copy this file to .env and fill in your actual credentials
# NOTE: Expo requires the EXPO_PUBLIC_ prefix for env vars to be accessible
# in the app. Do not remove this prefix!
# ============================================================================

# ----------------------------------------------------------------------------
# OAuth Configuration
# ----------------------------------------------------------------------------

# Google OAuth
# Get credentials from: https://console.cloud.google.com/apis/credentials
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
EXPO_PUBLIC_GOOGLE_CLIENT_SECRET=your-google-client-secret

# GitHub OAuth
# Get credentials from: https://github.com/settings/developers
EXPO_PUBLIC_GITHUB_CLIENT_ID=your-github-oauth-client-id
EXPO_PUBLIC_GITHUB_CLIENT_SECRET=your-github-oauth-client-secret

# Microsoft OAuth
# Get credentials from: https://portal.azure.com
EXPO_PUBLIC_MICROSOFT_CLIENT_ID=your-microsoft-oauth-client-id
EXPO_PUBLIC_MICROSOFT_TENANT=common

# ----------------------------------------------------------------------------
# Backend API Configuration
# ----------------------------------------------------------------------------

# Backend API Base URL
# - For iOS Simulator: use http://localhost:8000/api/v1
# - For Android Emulator: use http://10.0.2.2:8000/api/v1
# - For Physical Device: use your computer's local IP
#   (Find it with: ifconfig | grep "inet " | grep -v 127.0.0.1)
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# ----------------------------------------------------------------------------
# App Configuration
# ----------------------------------------------------------------------------

# App URL Scheme (must match app.json "scheme" field)
# Used for OAuth redirects: proxyagent://auth/google
EXPO_PUBLIC_APP_SCHEME=proxyagent
